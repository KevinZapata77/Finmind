spring:
  application:
    name: finmind
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/finmind?useSSL=false&serverTimezone=America/Bogota}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      # La instancia gratuita en la nube tiene 1 CPU y 1 GB de RAM.
      # Un pool grande la satura y empieza a rechazar conexiones.
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 900000
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  jackson:
    default-property-inclusion: non_null

server:
  port: 8080
  error:
    include-message: always
    include-stacktrace: never
    include-binding-errors: never

finmind:
  jwt:
    secret: ${JWT_SECRET:clave_de_desarrollo_solo_local_minimo_32_caracteres}
    expiration-ms: ${JWT_EXPIRATION_MS:3600000}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  level:
    com.finmind: DEBUG
