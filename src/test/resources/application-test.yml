spring:
  datasource:
    url: jdbc:h2:mem:finmind_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
finmind:
  jwt:
    secret: clave_de_pruebas_unicamente_minimo_32_caracteres
    expiration-ms: 3600000
